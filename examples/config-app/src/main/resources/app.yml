# config-app example: loader entries
plugins:
  - id: feature-a
    name: feature-plugin
    config:
      name: alpha
  - id: group
    name: group-plugin
    group: true
    config:
      - id: nested-feature
        name: feature-plugin
        config:
          name: nested
